import React from "react"
import './Offer.css'

export default function Offer(props)
{
    return <div id={props.id} className="offer">
        <font color="white">Big SALE ENERGY up to 25%</font>
    </div>
}